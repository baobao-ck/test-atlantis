version: 3
projects:
  - dir: .
    autoplan:
      when_modified: ["*.tf", "*.tfvars", "backend.tf"]
      enabled: true

  - dir: tools
    autoplan:
      when_modified: ["*.tf", "../modules/**/*.tf"]
      enabled: true
